DirectoryIndex index.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} !=on [OR]
  RewriteCond %{HTTP_HOST} ^www\.ijsbaannederbetuwe\.nl$ [NC]
  RewriteRule ^ https://ijsbaannederbetuwe.nl%{REQUEST_URI} [R=301,L,NE]
</IfModule>
